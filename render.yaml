services:
  - type: web
    name: qdrant-goods-categorization
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: QDRANT_URL
        sync: false
      - key: QDRANT_API_KEY
        sync: false
